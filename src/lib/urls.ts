/** Canonical reschedule page for all follow-up emails and SMS */
export const RESCHEDULE_URL = 'https://elystra.online/reschedule';

export function getRescheduleUrl(): string {
  return RESCHEDULE_URL;
}
